import axios from "axios";
import { useState } from "react";
import { Dimensions, Image, Modal, StatusBar, Text, View } from "react-native";




export const dWidth = Dimensions.get('window').width;
export const dHeight = Dimensions.get('window').height;

export const dsbar = StatusBar.currentHeight;
